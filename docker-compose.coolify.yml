version: '3.8'

services:
  dashboard:
    build: .
    ports:
      - "3000:3000"
    volumes:
      # Persistent storage for state - survives redeploys!
      - dashboard-data:/app/data
    restart: unless-stopped

volumes:
  dashboard-data:
    # This creates a named volume that persists across redeploys
